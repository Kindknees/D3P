experiment_name: matrix_A_no_disturbance_mechanism_ablation
output_root: outputs/adaptive_commitment/matrix_A
tasks: [lift, can, square]
seeds: [0, 1, 2, 3, 4]
episodes_per_seed: 100
methods:
  - fixed_chunk
  - td_replan_anywhere_random_reset
  - td_replan_boundary_only
  - phase_aware_hysteresis
  - adaptive_commitment_heuristic
  - td_repair_residual_050
  - adaptive_commitment_with_repair
action_noise:
  enabled: false
logging:
  save_video: false
  save_step_logs: true
  save_event_logs: true
